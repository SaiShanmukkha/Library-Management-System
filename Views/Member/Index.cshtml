@model IEnumerable<LibrarySystem.Models.Member>


<div class="container p-3">
    <div class="row p-4">
        <div class="col-6">
            <h2 class="text-primary">Members List:</h2>
        </div>
        <div class="col-6 text-right">
            <a asp-controller="Member" asp-action="Create" class="btn btn-primary">Add New Member</a>
        </div>
    </div>
    <br />

    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-secondary table-striped">
            <thead>
                <tr>
                    <th>
                        Name
                    </th>
                    <th>
                        Email
                    </th>
                    <th>
                        Phone Number
                    </th>
                    <th>
                        Actions
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var member in Model)
                {
                    <tr>
                        <td width="25%">@member.MemberName</td>
                        <td width="25%">@member.Email</td>
                        <td width="25%">@member.PhoneNumber</td>
                        <td width="25%">
                            <div class="text-center btn-group w-100">
                                <a class="btn btn-primary mx-1 text-white" asp-controller="Member" asp-action="Update" asp-route-Id="@member.MemberId">Update</a>
                                <a class="btn btn-danger mx-1 text-white" asp-controller="Member" asp-action="Delete" asp-route-Id="@member.MemberId">Delete</a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p class="text-center">No Members</p>
    }
    </div>